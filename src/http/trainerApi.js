import { api } from "./index";

// export const trainerCancelSlot = async (slot) => {
//   // В слоте бдует хранится trainer_id, user_id, date,

//   try {
//     const response = await api.post("/trainer_cancel", slot, {
//       headers: { "Content-Type": "application/json" },
//     });

//     return response;
//   } catch (error) {
//     console.error("Error while canceling slot");
//   }
// };
